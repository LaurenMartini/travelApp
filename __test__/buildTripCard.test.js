import { buildTripCard } from '../src/client/js/buildTripCard'

describe("Testing build trip card", () => {
    test("Testing the buildTripCard() function", () => {
        expect(buildTripCard).toBeDefined();
    })
});