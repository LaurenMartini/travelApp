import { generateTripView } from '../src/client/js/generateTripView'

describe("Testing generate trip view", () => {
    test("Testing the generateTripView() function", () => {
        expect(generateTripView).toBeDefined();
    })
});